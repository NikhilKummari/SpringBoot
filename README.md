# SpringBoot
Spring FrameWork without using SpringBoot
This project will let you know what's exactly happening behind the scenes
when using SpringBoot we use annotations (@Component, @Autowired ...) those makes our work simple but to know what is happening we are working on this project with Spring.